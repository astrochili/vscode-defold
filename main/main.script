local debugger = require('debugger.debugger')
debugger.start()

-- This is an example script to debug

local function sum(a, b)
  return a + b
end

function init(self)
  local a = 1
  local b = 2
  local c = sum(a, b)
end